<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- <h1 class="text-3xl-yellow ">Hello in this mini project</h1> -->

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 bg-red-500 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic%>" alt="">
            </div>

            <h3 class="text-3xl mb-5"> <span class="font-light"> &nbsp;Hello</span>,  <%= user.name %> </h3>
            
        </div>
        <h5 class="mb-5">You can create a new post</h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none" placeholder="What's on your mind" name="content"></textarea>
            <input class="text-sm px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40" type="submit" value="Create New Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="post-container mt-5 rounded-md">
                <% if (user.post && user.post.length > 0) { %>
                    <% user.post.reverse().forEach(function(post) { %>
                        <div class="post w-1/3 mb-5 p-4 bg-zinc-900 border-2 border-zinc-700 rounded-md">
                            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                            <p class="text-sm tracking-tight"><%= post.content %></p>
                            <small class="mt-2 inline-block"> <%=post.like.length%> likes</small>
                            <div class="btns flex gap-4 mt-1">
                                <a href="/like/<%=post._id%>" class="text-blue-500">
                                    <%= post.like.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                </a>
                                <a href="/edit/<%=post._id%>" class="text-yellow-500">Edit</a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-zinc-400">No posts to display.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>

<!-- 1   2   3   4   5   6 -->